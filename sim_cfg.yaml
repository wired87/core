# Apply energy param changes to fermions to detect gauge interaction changes
# -> psi describes jnu so jnu is higher in hierarchy
# env-rajtigesomnlhfyqzbvx-qyhimnidxniwbmlzhlqy-77856c7654-m78pp
sim_time_s: 300 # no matter how long, everything gets split into sub blocks for easier handling
cluster_dim:
  - 1
  - 1
  - 1
resources:
  device: cpu
  cpu: 6
  cpu_limit: 6
  gpus: 0               # GPU-Anzahl (falls device=gpu)
  mem: 12Gi       # max. RAM für die Sim
  mem_limit: 12Gi       # max. RAM für die Sim
