# Todo

## Implemented

- [x] **Implement Google Lens for equation detection (use gs-ai API)**  
  - Module: `qbrain/core/file_manager/google_lens_equations.py`  
  - Uses `GS_AI_API_KEY` and optional `GS_AI_BASE_URL` (default: SearchAPI Google Lens–compatible endpoint).  
  - Accepts image URL → returns `{ "latex": [], "math_elements": [], "equations": [] }` parsed from Lens results.

## Pending

(Add new items below.)
