within session manager implmeent a 
research_files-col whcih receives a list[str] urls param. 
use the self.session_id and user_id params to receive 
all entries from the upserted list. locally merge both 
lists and  overwrite the specific col wit the result. 
implemnt this method within te orchestrator research 
thread callback method workflow. 


